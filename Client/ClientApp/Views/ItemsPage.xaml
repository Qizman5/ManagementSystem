<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ClientApp.Models"
             x:Class="ClientApp.Views.ItemsPage"
             Title="Список товарів">

    <Grid RowDefinitions="Auto,*,Auto" Padding="15">
        <Button Grid.Row="0" Text="Оновити список" Command="{Binding LoadItemsCommand}" Margin="0,0,0,10" />

        <CollectionView Grid.Row="1" ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Item">
                    <Frame Margin="0,5" Padding="10" CornerRadius="8">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding Description}" FontSize="14" TextColor="Gray" />
                            <HorizontalStackLayout Spacing="15">
                                <Label Text="{Binding Quantity, StringFormat='Кількість: {0}'}" />
                                <Label Text="{Binding Price, StringFormat='Ціна: {0:C}'}" FontAttributes="Bold" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2" Text="Створити операцію" Command="{Binding GoToActionCommand}" Margin="0,10,0,0" />
    </Grid>
</ContentPage>